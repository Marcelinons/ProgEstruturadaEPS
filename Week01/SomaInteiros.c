#include <stdio.h>

/// @brief Receives two ints, a and b, prints them and their sum.
void main() {
    int a, b;
    scanf("%d%d", &a, &b);
    printf("Alunos: %d\nAlunas: %d\nTotal: %d", a, b, a+b);
}
